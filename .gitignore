# Local build outputs / symlinks
result
result-*
modules/result

# Local downloaded/proprietary blobs
displaylink-file
displaylink-*.zip

# Installer runtime state (generated per machine)
hosts/*/.installer-state.json
hosts/*/.nhl-enrolled
hosts/*/.hardware-probe.json
hosts/*/.install-log

# Local helper state
GTK-themes-icons/
NixOS-Hyprland-backups/
*.hm-bak
.codex
.codex/
.aider.conf.yml

# Python cache / bytecode
__pycache__/
*.pyc
.aider*
